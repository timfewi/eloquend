[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "eloquend"
version = "0.1.0"
description = "Low-latency, local streaming TTS daemon"
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
eloquend = "eloquend.__main__:main"

[tool.setuptools.packages.find]
where = ["src"]
